<div class="collection-container">
  <% @works.each do |work| %>
    <div class="work-container">
      <div class="work">
        <%= image_tag work.image.url %>
      </div>
    </div>
  <% end %>
  <%= paginate @works %>
</div>